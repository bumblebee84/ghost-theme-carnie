<div class="card">
    <div class="card-image">
        <figure class="image">
            <img src="{{feature_image}}" alt="Placeholder image">
        </figure>
    </div>
    <div class="card-content">
        <div class="media">
            <div class="media-left">
                <figure class="image is-48x48">
                    {{#primary_author}}
                        <a href="{{url}}" class="has-text-dark"><img src="{{profile_image}}" alt="{{name}}'s Picture"/></a>
                    {{/primary_author}}
                </figure>
            </div>
            <div class="media-content">
                <p class="title is-4"><a href="{{url}}" class="has-text-dark">{{title}}</a></p>
                <p class="subtitle is-6">{{#primary_author}}<a href="{{url}}">@{{name}}</a>{{/primary_author}}</p>
            </div>
        </div>

        <div class="content">
            <a href="{{url}}" class="has-text-dark">{{excerpt}}</a>
            {{tags}}
            <br>
            <time datetime="2016-1-1">{{date published_at timeago="true"}}</time>
        </div>
    </div>
</div>